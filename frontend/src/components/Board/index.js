import React from 'react'
import { Grid } from '@mui/material'

function Board() {
    return (
        <Grid container item xs={9} style={styles.board}>
            
        </Grid>
    )
}

const styles = {
    board: {
        backgroundColor: '#C3DFDF'
    }
}

export default Board
